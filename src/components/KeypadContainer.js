import React from "react";
import "./KeypadContainer.css";

const KeypadContainer = ({ children }) => {
  return <div className="keypad-container">{children}</div>;
};
export default KeypadContainer;
